name: PHP Composer

on:
  push:
    branches: [ testBranch ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@testBranch

    - name: Create database
      run: mysql -e 'CREATE DATABASE doctrine;'

    - name: Travis setup
      run: .travis/setup

    - name: Run Travis
      run: .travis/run_tests.sh
